#!/bin/bash

EXE="vim"
SESSION="./session"

main()
{
    files=($(eval echo $(\cat "${SESSION}" | grep '^[^#]' | sed "s/['\"]/\\\&/g"))) || exit ${?}
    ${EXE} -- "${files[@]}" || exit ${?}
}

main "$@"
